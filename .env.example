# Database connection (required for production)
DATABASE_URL=postgresql://beans:YOUR_PASSWORD@localhost:5432/beans

# Flask secret key (generate with: python3 -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=change-me

# Set to "production" to disable debug mode
FLASK_ENV=production
