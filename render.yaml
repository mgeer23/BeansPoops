databases:
  - name: beans-db
    plan: free
    databaseName: beans
    user: beans

services:
  - type: web
    name: beans-potty-log
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: beans-db
          property: connectionString
